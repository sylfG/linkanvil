# [UI / PRESENTATION] F-04.5 — Function Calling Activo y Soberano del LLM al histórico RAG crudo almacenado para rebatir ambigüedad a petición expresa

**Épica:** EPIC-04 — Interacción, Memoria Híbrida y Control

> ⚠️ **Estado actual: NOT_STARTED** — el endpoint `/chat` no envía `tools=[]` ni
> interpreta `tool_calls` (grep negativo en `src/api/main.py`). El test
> `tests/test_f045_function_calling.py:12` está `@pytest.mark.skip` con motivo
> "Pendiente refactor a usuario_recursos tras migración 0002". El RAG actual ya
> inyecta chunks relevantes en el sysprompt — si esto se considera suficiente,
> la ficha podría marcarse OBSOLETE en una próxima revisión.

## 🏷️ Categoría

**Categoría:** `UI / Presentation`
**Impacta en:** Equipo End-to-End

## 📦 Dependencias

> Backlogs que deben estar **completados** antes de implementar esta feature.

| Backlog | Motivo | Bloqueante |
| --- | --- | --- |
| [F-04.4](F-04.4_anterior.md) | Dependencia en cascada. | Sí |

**Prioridad:** `COULD`

---

**Descripción:**
Como Actor
Quiero **Function Calling Activo y Soberano del LLM al histórico RAG crudo almacenado para rebatir ambigüedad a petición expresa**
Para que la funcionalidad orientada a eventos opere.

**Criterios de Aceptación (Checklist):**

- [ ] **Happy Path (no implementado):**
  **Dado que** el usuario envía un `POST /chat` con una consulta ambigua,
  **Cuando** el endpoint construya la petición a LiteLLM,
  **Entonces** debería incluir `tools=[{"type": "function", "function": {"name": "search_raw_history", "parameters": {...}}}]`
  y, ante un `tool_calls` en la respuesta, ejecutar la búsqueda contra el
  histórico crudo (`mensajes_chat`/`recursos`) y reinyectar el resultado
  en una segunda iteración del LLM.
  **Estado**: el código actual NO envía `tools=` ni `tool_choice` en
  ninguna llamada a LiteLLM (grep negativo en `src/api/main.py`).

- [ ] **Edge Case (no implementado):**
  **Dado que** el LLM o el puente (LiteLLM) caen mientras se resuelve
  un `tool_call`,
  **Cuando** se intenta ejecutar,
  **Entonces** debería derivar a DLQ o ejecutar Fallback (RAG estándar
  sin tools). Hoy no aplica — no hay `tool_calls` que resolver.

- [ ] **Requisito Técnico (no implementado):**
  - Sin código de tools/function-calling presente en `src/api/main.py`.
  - Test `tests/test_f045_function_calling.py::test_f045_function_calling_historico_crudo`
    está `@pytest.mark.skip(reason="Pendiente refactor a usuario_recursos tras migración 0002")`.
  - Para cerrar la ficha: definir la tool `search_raw_history(query: str)` que
    busque en `mensajes_chat` filtrando por `tenant_id` vía JWT, instrumentar
    trazas OTel con `trace_id` y `tenant_id`, y reactivar el test.

---

**MoSCoW:** `COULD`
**Sprint:** Sprint N

**Notas:**

- Generado según Fase 3.

## 📝 Notas de implementación

**Estado**: NOT_STARTED (verificado 2026-05-19)
**Archivos relevantes (gap)**:
- `src/api/main.py` — no contiene `tools=` ni `tool_choice` en ninguna llamada LiteLLM.
- `tests/test_f045_function_calling.py:12` — test skipped indefinidamente.

**Decisión pendiente**:
- Confirmar si el RAG estándar (chunks Qdrant inyectados en sysprompt) ya
  cubre el objetivo de "rebatir ambigüedad", en cuyo caso la ficha pasaría
  a **OBSOLETE**. Si se mantiene, abrir tarea para implementar la tool
  `search_raw_history` con su test.

---

## 📋 Notas del v2 (generado por backlog-reviser · 2026-05-19)

**Origen**: `/tmp/linkanvil-mirror/docs/src/Extractor_de_Requisitos/backlog/F-04.5_function-calling-activo-y-sobe.md`
**Hallazgo en implementation-status.md**: "NOT_STARTED — El test `tests/test_f045_function_calling.py` está skipped (`Pendiente refactor a usuario_recursos tras migración 0002`). El endpoint `/chat` no usa `tools=[]` ni `tool_choice` (grep negativo en `src/api/main.py`); el LLM no tiene function calling activo contra el histórico RAG crudo."

### Cambios aplicados
- AC reescritos (3 items): marcados como no implementados con detalle del gap concreto (no `tools=`, no `tool_choice`, test skipped).
- Notas de implementación añadidas: gap path:línea + decisión pendiente entre cerrar como OBSOLETE (cubierto por RAG estándar) o abrir tarea concreta.
- Estado actualizado: IMPLEMENTED (asumido por la plantilla) → **NOT_STARTED**.

### Pendientes (no aplicados)
- Decisión bloqueada: requiere input del PO sobre OBSOLETE vs. roadmap.
